
on:
  pull_request:
    types: [opened, reopened, synchronize]

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Echo test
        run: |
          echo "pull_request workflow is running..."
          echo "Event: ${{ github.event_name }}"
          echo "Actor: ${{ github.actor }}"
